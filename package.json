{
  "name": "fizbuz-api",
  "version": "0.0.1",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express-jwt": "^5.3.3",
    "jwks-rsa": "^1.8.0",
    "nexus": "0.21.0",
    "nexus-plugin-prisma": "^0.8.0"
  },
  "scripts": {
    "format": "prettier --write './**/*.{ts,md}'",
    "dev": "nexus dev",
    "build": "nexus build",
    "seed": "ts-node prisma/seed.ts",
    "start": "prisma migrate save --experimental && prisma migrate up --experimental  && node node_modules/.build"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "prettier": "^2.0.5"
  }
}
